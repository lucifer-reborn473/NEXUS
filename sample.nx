var limit = 4 * 10^6;
    var a = 1;
    var b= 2;
    var sum = 0;
    while (b < limit){
        if (b%2==0) then{
            sum+=b;
        }
        end;
        var temp = a + b;
        a = b;
        b = temp;
    };
    displayl sum;